# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and file
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.production
.git-credentials
dev

# Data folder (generated by collect script, don't track in git)
# Only track when deployed via GitHub Actions
data

# Symlink to data folder (tracked as symlink)
# public/data -> ../data

# Tesseract OCR language data (downloaded at runtime by the Fiat OCR step)
*.traineddata